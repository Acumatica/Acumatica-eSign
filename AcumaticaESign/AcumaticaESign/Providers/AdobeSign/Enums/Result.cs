namespace AcumaticaESign
{
    public enum Result
    {
        REMINDER_SENT,
        CANCELLED,
        ALREADY_SIGNED,
        ALREADY_CANCELLED,
        INVALID_DOCUMENT_KEY,
        INVALID_API_KEY,
    }
}
